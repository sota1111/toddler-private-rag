# SOT-1359: slim deps for the gen2 upload Cloud Function (backend/upload_function/main.py).
# Deliberately excludes OCR/AI deps (pytesseract, pypdf, pdf2image, Pillow, google-cloud-vision,
# google-genai) and FastAPI so the function stays small and cold-starts quickly. OCR/enrich runs
# on the heavy backend (AI worker), triggered over HTTP.
functions-framework==3.*
google-cloud-storage>=2.10.0
google-cloud-firestore>=2.11.0
requests>=2.34.2
