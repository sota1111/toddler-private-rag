# SOT-1322: minimal deps for the lightweight upload-only service (backend/Dockerfile.upload).
# Deliberately excludes OCR/AI deps (pytesseract, pypdf, pdf2image, Pillow, google-cloud-vision,
# google-genai) so the image stays small and boots fast. Versions match requirements.txt.
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
python-dotenv==1.0.1
python-multipart==0.0.9
httpx==0.27.2
google-cloud-storage>=2.10.0
google-cloud-firestore>=2.11.0
